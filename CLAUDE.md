## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on this repo (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
